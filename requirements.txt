# Core runtime dependencies for the TF-IDF + Naive Bayes pipeline, FastAPI, and
# the Streamlit UI. Summarization and insight extraction are pure-stdlib regex,
# so no spaCy/NLTK is required. Project metadata + dev deps live in pyproject.toml.
fastapi>=0.100.0
uvicorn>=0.23.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0
plotly>=6.7.0
streamlit>=1.28.0
pydantic>=2.13.4
